apiVersion: v1
kind: Service
metadata:
  name: grafana-deployment-xfusion
  labels:
    app: grafana-deployment-xfusion
spec:
  type: NodePort
  selector:
    app: grafana-deployment-xfusion
  ports:
  - port: 3000
    targetPort: 3000
    nodePort: 32000
    protocol: TCP